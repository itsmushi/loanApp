import 'package:flutter/material.dart';

class MicrofinanceScreen extends StatelessWidget {
  const MicrofinanceScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("microfinance"),
    );
  }
}
